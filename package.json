{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "clean": "lerna run clean && rimraf node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "lerna": "^8.1.8",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  }
}
